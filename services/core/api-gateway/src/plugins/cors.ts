// import fp from "fastify-plugin";
// import type { FastifyInstance } from "fastify";
// import cors from "@fastify/cors";

// export default fp(
//   async (app: FastifyInstance) => {
//     await app.register(cors, {
//       origin: app.config.CORS_ORIGIN as string,
//       credentials: true,
//     });
//   },
//   { name: "cors" },
// );
